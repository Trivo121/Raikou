[Service]
# Only the local Nginx gateway may contact Uvicorn.  The EC2 security group
# therefore never needs to expose port 8000 or the model-serving process.
ExecStart=
ExecStart=/home/ubuntu/sar_env/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8000
